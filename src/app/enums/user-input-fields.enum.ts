export enum AgUserInput {
  X_INTERVAL = 'xInterval',
  Y_INTERVAL = 'yInterval',
  MIN_IMG_WIDTH = 'minImgWidth',
  ROW_HEIGHT = 'rowHeight',
  ANIMATION = 'animation',
  BOX_SHADOW = 'boxShadow',
  BORDER_RADIUS = 'borderRadius',
  FILTER = 'filter'
}
